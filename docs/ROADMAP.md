# خارطة الطريق

## أُنجز مؤخرًا (v1.2 / v1.3)
- [x] **إصلاح الدخول على الهاتف:** اتصال هجين — fetch مباشر إلى `doPost` (بلا كوكيز طرف ثالث) مع جسر iframe كاحتياط.
- [x] **نظام تشخيص على الهاتف:** `debug.js` + Eruda، مع لوحة سجل (نسخ/مشاركة/تكبير/تصغير) تُفعَّل بـ `?debug=1`.
- [x] **تقارير الأخطاء بالبريد:** أي خطأ لدى أي مستخدم يصل المالك تلقائيًا عبر `logClientError` (مع منع التكرار وسقف إرسال).
- [x] **استمرارية الجلسة:** التحديث لم يعد يسجّل الخروج (جلسة محفوظة محليًا ضمن حدّ الخمول).
- [x] **معالجة أعطال الواجهة:** لوحة المؤشرات والسجل تعرضان رسالة خطأ وزر إعادة محاولة بدل التعليق على شاشة التحميل.
- [x] **رسائل حفظ أوضح:** «تم حفظ التعديل» عند التعديل، وتحديث القائمة بعده.
- [x] **تصدير Excel من السجل:** ملف منظّم حسب الفترة المختارة، مع اسم المعدّ وتاريخ الإعداد وتنزيل مباشر.

## التالي مباشرةً (يحتاج أصولًا منك)
- [ ] **التصدير المنظّم بالصور (PDF):** تخطيط مرن — صورتان نصف صفحة، ٥ صور شبكة، إلخ — مع بيانات الفعالية واسم المنفّذة والتاريخ والشعار. (يحتاج: الشعار في assets/logo)
- [ ] **قالب Excel الرسمي للمديرية:** ربط التصدير الحالي بقالب «الشراكات» و«المبادرات» عند توفر ملف xlsx الرسمي.

## تحسينات مقترحة
- [ ] قراءة قائمة المنفّذين آليًا من ورقة Users بدل القائمة الثابتة في النموذج.
- [ ] مصادقة بـ PIN قصير لكل مستخدم (تعزيز بسيط للأمان).
- [ ] ترقيم/تحميل تدريجي للسجل عند تجاوز ~٥٠٠ سجل.
- [ ] رسوم بيانية للمؤشرات (Chart) بدل الأشرطة البسيطة.
- [ ] تنظيف الصور اليتيمة في Drive عند حذف صورة من فعالية.
- [ ] ورقة شراكات مستقلة بربع سنوي (Q1/Q2…) تُملأ من ربط الفعاليات بالشراكة.
- [ ] تصفية إضافية بالسجل (حسب النوع/المنفّذة) وبحث نصّي.
- [ ] نسخ احتياطي دوري لجدول البيانات.
